[package]
name = "Maifinaletodxrust"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serialport = "4.2.0"
clap = { features = ["derive"] }
crossbeam-channel = "0.5.7"
toml = "0.7.3"
